{
  "name": "nuxt-firebase",
  "version": "1.0.0",
  "description": "nuxt firebase template",
  "author": "comfuture",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "nuxt": "^3.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
